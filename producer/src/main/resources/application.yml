server:
  port: 8082
spring:
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      missing-topics-fatal: false

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    path: /api

producer:
  kafka:
    topic-name: tracing

